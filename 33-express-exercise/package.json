{
  "name": "33-express-exercise",
  "version": "1.0.0",
  "description": "create an express app that fetches the users list from https://dummyjson.com/users filter names according to a query param `filter` returns either json or xml depending on query param `format` (default json)",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
  }
}
