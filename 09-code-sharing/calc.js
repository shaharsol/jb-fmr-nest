// CommonJS

const sum = (a, b) => a + b
const sub = (a, b) => a - b
const mul = (a, b) => a * b
const div = (a, b) => a / b

const message = "hello i am a message"

module.exports = {
    sum,
    sub,
    mul,
    div,
    message
}