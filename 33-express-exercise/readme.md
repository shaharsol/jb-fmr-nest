create an express app that
fetches the users list from https://dummyjson.com/users
filter names according to a query param `filter`
returns either json or xml depending on query param `format` (default json)


get query params in express : request.query.filter